
sudo useradd -m -d /home/guest -c 'comment' -s /bin/bash -G adm,mail user:
sudo chage -l guest    -->  to view user expiration date
sudo usermod -c guests guest

sudo groupadd developers
sudo usermod -G developers guest  --> removing user from previous groups
sudo usermod -aG developers guest  --> appending developers groups to guest previous groups
sudo userdel -r guest	--> delete user along with its home directory

